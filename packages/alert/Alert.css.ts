import { createSprinkles } from '@vanilla-extract/sprinkles'
import { box, radius, elevation, palette } from '../theme/sprinkles.css'

export const sprinkles = createSprinkles(box, radius, elevation, palette)

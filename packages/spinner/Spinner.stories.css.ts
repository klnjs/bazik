import { sprinkles } from '../../.storybook/src/style/sprinkles.css'

export const spinner = sprinkles({ display: 'block' })

export const thumb = sprinkles({
	color: 'teal11'
})

export const track = sprinkles({
	background: 'teal3'
})

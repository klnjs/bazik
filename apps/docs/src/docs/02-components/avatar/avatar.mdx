import { Features, Feature } from '@site/src/lib/features'
import { Installation } from '@site/src/lib/installation'
import { ReferenceProps } from '@site/src/lib/reference'
import { Showcase } from '@site/src/lib/showcase'
import Avatar from './avatar'
import source from '!!raw-loader!./avatar.tsx'
import styles from '!!raw-loader!./avatar.module.css'

# Avatar

An element for representing a user.

## Showcase

<Showcase
	name="avatar"
	sources={[
		{ file: 'index.tsx', content: source, language: 'jsx' },
		{ file: 'index.module.css', content: styles, language: 'css' }
	]}
>
	<Avatar />
</Showcase>

## Features

<Features>
	<Feature>Image loading status handling.</Feature>
	<Feature>Fallback to any content imaginable.</Feature>
	<Feature>Delay fallback to avoid flickering.</Feature>
</Features>

## Installation

<Installation name="avatar" />

## Reference

### Avatar (Root)

Contains all the parts of an avatar.

<ReferenceProps
	props={[
		{ name: 'asChild', type: 'boolean', defaultValue: 'false' },
		{
			name: 'onStatusChange',
			type: 'function',
			typeAdvanced:
				"(status: 'idle' | 'loading' | 'loaded' | 'error') => void"
		}
	]}
/>

### AvatarImage

Used to render the image. The image is hidden until it has loaded. Use the root property onStatusChange, to listen to the image status.

<ReferenceProps
	props={[
		{ name: 'asChild', type: 'boolean', defaultValue: 'false' },
		{ name: 'src', type: 'string' }
	]}
/>

### AvatarFallback

Used to render the fallback while the image is loading, or when an error occurs. To prevent flickering between image and fallback use the `delay` property, to delay rendering of the fallback whilst image is loading.

<ReferenceProps
	props={[
		{ name: 'asChild', type: 'boolean', defaultValue: 'false' },
		{ name: 'delay', type: 'number' }
	]}
/>
